[build]
  functions = "netlify/functions"
  command   = "if [ -d netlify/functions ]; then cd netlify/functions && npm install; fi"

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
```

**3 —** Commit

---

**4 —** Ensuite créez le fichier manquant — sur GitHub → **Add file → Create new file** → tapez :
```
netlify/functions/create-checkout.js
